version: '3.8'
services:
  payment-api:
    image: paymentapi:v3
    container_name: paymentapi
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
